describe('Divisors', function() {
   it('it returns an array of divisors', function() {
      expect(divisors(10)).toEqual([2,5]);
   });
   
   /*it('says hello to someone', function() {
      expect(hello('Abel')).toEqual('Hello, Abel!');
   });*/
});